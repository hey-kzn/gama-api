import { Controller } from '@nestjs/common';

@Controller('expenses')
export class ExpensesController {
  // ajouter une dépense
  //  il faut le type de la dépense
  //  la catégorie de la dépense
  //  payer comment ? (type de payement)
}
